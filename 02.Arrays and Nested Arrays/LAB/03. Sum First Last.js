function sumFirstLast(arr){

    // let firstEl=Number(arr.shift());
    // let last=Number(arr.pop());
    // let sum=firstEl+last;


    // console.log(sum);
    // arr.map(Number)

    console.log(Number(arr.shift()) + Number(arr.pop()));

}
sumFirstLast(['20', '30', '40']);